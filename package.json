{
  "name": "hchiam.github.io",
  "version": "1.0.0",
  "dependencies": {},
  "scripts": {
    "test": "jest",
    "minify": "minify style.css > minified-style.css; minify brain.js sound.js cursor.js > minified-code.js;",
    "build": "yarn run minify || npm run minify; bash copy-to-public-folder.sh;",
    "start": "node index.js & open http://localhost:8000",
    "stop": "killall -9 node",
    "continuous-restart": "nodemon -x 'yarn build; node index.js;' --ignore public --ignore 'minified*.*';"
  },
  "eslintConfig": {},
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "express": "^4.17.1",
    "husky": "^4.0.10",
    "jest-cli": "^24.9.0",
    "minify": "^4.1.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "echo; echo 'Reminders: Did you build first? Do you need to [skip ci]?'; echo; npm test; echo; echo 'Reminders: Did you build first? Do you need to [skip ci]?'; echo;"
    }
  },
  "license": "MIT"
}
