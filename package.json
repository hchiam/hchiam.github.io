{
  "name": "hchiam.github.io",
  "version": "1.0.0",
  "dependencies": {},
  "scripts": {
    "test": "jest",
    "minify": "minify style.css > minified-style.css; minify brain.js sound.js > minified-code.js;",
    "build": "npm run minify; bash copy-to-public-folder.sh;",
    "start": "node index.js"
  },
  "eslintConfig": {},
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "express": "^4.17.1",
    "jest-cli": "^24.9.0",
    "minify": "^4.1.3"
  }
}
