{
  "name": "hchiam.github.io",
  "version": "5.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && astro export && npm run deploy:pre-deploy && npm run deploy:surge && npm run deploy:gh-pages",
    "deploy:pre-deploy": "cp {.nojekyll,CNAME,manifest.webmanifest} dist",
    "deploy:surge": "surge out https://hchiam-test.surge.sh",
    "deploy:gh-pages": "git subtree push --prefix dist origin test-main-as-gh-pages"
  },
  "devDependencies": {
    "@astrojs/react": "^0.1.3",
    "astro": "^1.0.0-beta.42",
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  }
}
